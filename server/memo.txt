- 라우터 연결

-- member
"로그인" 과 "회원가입" 은 '/member' 라우터를 이용하여 연결
member_router 상에서 /api/member/login, /api/member/signup 등

-- riding, bike
1. /riding/start
2. /routes
3. /riding/routes/:id

4. /bike/seoul
5. /bike/seoul/stations
6. /bike/seoul/analysis
7. /bike/seoul/forecast
"riding"과 "bike"의 라우터를 합쳐 진행하려고 하였으나, 서로 분리된 페이지인 것을 확인하고 중단.
다만, /bike/seoul에서 원하는 루트를 선택하면 /riding/routes/id로 넘어간다.

-----------------------------------------------

-----------------------------------------------

-- dashboard
로그인 이후 회원정보를 뿌려주는 페이지

로그인이 완료되어도 토큰이 저장되지 않아 페이지 이동 시, 로그인 상태가 유지되지 않는다. -> 토큰을 캐시에 저장하도록 서버 연결


------------------------------------------------------------------------------------
2026.08.12
1. 회원가입 FastAPI 라우터 + DB 연동
- 회원가입 라우터 구성
- 회원가입에 필요한 member schema.py와 model.py 형성
- schema 내에서 추가적인 validation check 진행 로직 추가
    - @model_validator를 이용하여 validation check 진행
        - 비밀번호가 일정 길이 이상인가
        - 비밀번호와 비밀번호 확인이 일치하는가
        - 필수 약관에 동의 체크를 하였는가
- model 내에서 다양한 입력 형식을 추가
    - JSON 방식
    - Boolean 방식
- member_router 내에서 비밀번호 암호화
    - core.security.py -> hash_password를 이용해 입력된 비밀번호 암호화

2. 로그인 FastAPI 라우터 + DB 연동
- 로그인 라우터 구성
- 로그인에 필요한 MemberItem 클래스 정의
- 암호화 된 비밀번호와 로그인을 시도하는 비밀번호의 일치 여부 체크
- 닉네임과 비밀번호가 일치하지 않을 시, 로그인 거부

3. 로그아웃 FastAPI 라우터
- 로그아웃 라우터 구성

------------------------------------------------------------------------------------

2026.08.13
- 회원가입 후 계정 정보 DB에 저장 연동

------------------------------------------------------------------------------------

2026.08.17
- 로그인 후 /dashboard 로 라우팅
    대시보드에 필요한 라우터, 스키마, 모델 생성
    dashboard_model을 이용해 새로운 "member_status"라는 대시보드에 필요한 데이터 테이블 형성
        "bike_member"와 "member_status"는 "nickname"이라는 PK로 연결
    회원가입 후 "bike_member"테이블에 저장된 정보가 승인되어 로그인에 성공하였을 시,
    "member_status"에 기본값으로 초기화 된 데이터가 등록된다.

    로그인 라우터 쪽에서 이메일 정보를 받아오기 위하여 db.get() 메서드를 활용하였지만,
    PK는 "nickname"이지만 로그인에는 "email"을 사용하기 때문에
    PK만을 받아오는 db.get() 메서드가 아닌 db.query() 메서드로 변경
        이로 인해, 이메일 정보는 로그인에만 활용되고, 대시보드에 넘어올 때 닉네임 정보를 뿌려준다

------------------------------------------------------------------------------------

2026.08.18
- 대시보드 내에 뿌려줄 데이터 테이블 연동
- 로그인 후 대시보드에서 다른 페이지로 이동할 경우 로그인 (사용자) 데이터 저장
- 따릉이 라우팅
- route 라우터 / 스키마 / 모델 생성 및 seed_route.py를 이용해 db 내에 코스 데이터 (정규 데이터) 저장
- 따릉이 대여소에 대한 정규 데이터 seed_route.py를 이용해 db 내에 저장

------------------------------------------------------------------------------------

2026.08.19
- 개인 자전거 페이지 라우팅
- 개인 자전거 루트 페이지 데이터 API 연동
- DB 내 safety_tips 컬럼 결측치를 모두 같은 데이터로 업데이트

- 데이터 분석
    대여소 정보 데이터 (엑셀파일) 정리 및 필요한 데이터만 유지
    거치대수에서 1049건의 결측치가 발생
    중간값 10에 평균값 11.87로 측정되어 중간값을 채택

- 데이터 크롤링 완료
    - 공공자전거 대여소 위치 데이터
    - 월별 공공자전거 이용 데이터
    - 일별 공공자전거 이용 데이터
    - 시간별 공공자전거 이용 데이터

- 공공자전거 대여소 위치 데이터 DB 저장 완료
- 내 위치정보를 받아오는 GPS 연동이 불가능하여, 서울시청을 기준으로, 공공자전거 대여서 위치 데이터 내의
 위도, 경도 데이터를 이용해 가까운 위치의 6개 곳의 대여소 위치를 웹페이지에서 뿌려준다.
 실시간 대여 현황은 AI로 예측하려 하였으나, 당장은 랜덤한 새로고침을 진행할 때마다 랜덤한 숫자가 출력되도록 하였다.